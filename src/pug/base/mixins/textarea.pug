mixin textarea(inputs)
  each input, index in inputs
    label.mt-4(for=inputs.id)= input.label
    if input.require
      span.text-danger.ms-1.pt-2 *
    textarea.mt-2.p-2(class="form-control" rows="10" id=inputs.id)
